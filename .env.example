# Copy to .env and ensure Postgres + Redis are running (e.g. docker-compose up -d).
# Required for DB pages (/queue, /case/[id], etc.) and capture queue.
DATABASE_URL="postgresql://beacongate:beacongate_dev@localhost:5432/beacongate"
REDIS_URL="redis://localhost:6379"
EVIDENCE_STORAGE_DIR="./storage/evidence"

# Optional: deterministic test/LLM behaviour
# LLM_PROVIDER_FORCE=mock
# EMBEDDING_PROVIDER=local
